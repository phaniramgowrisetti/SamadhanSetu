import { renderCitizenWelcomeView } from './CitizenWelcomeView.js';

/**
 * Citizen Dashboard View (My Reports)
 * Re-exports the citizen problem reporting dashboard matching the reference image 1-to-1.
 */
export function renderMyReportsView() {
  return renderCitizenWelcomeView();
}
